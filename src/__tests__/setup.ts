// Redireccion a setup modular — ver src/__tests__/setup/ para el codigo real.
// Este fichero se conserva por si algun import externo lo referencia, pero
// vitest.config.ts ya carga setup/env.ts como setupFiles.
export {};
